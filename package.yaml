name: toit-gen
description: Generate Toit code.
dependencies:
  fs:
    url: github.com/toitlang/pkg-fs
    version: ^2.3.1
  host:
    url: github.com/toitlang/pkg-host
    version: ^1.17.0
environment:
  sdk: ^2.0.0-alpha.190
